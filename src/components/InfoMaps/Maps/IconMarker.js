import L from "leaflet";
import iconUrl from "../../../assets/img/logo-web-white.png";

const iconTroy = new L.Icon({
	iconUrl: iconUrl,
	iconSize: [45, 64],
	iconAnchor: [45, 64],
	shadowUrl: null,
	shadowSize: null,
	shadowAnchor: null,
});

export { iconTroy };